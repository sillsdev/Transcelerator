#!/bin/bash
# server=build.palaso.org
# build_type=Transcelerator_TransceleratorWinDefaultPullRequests
# root_dir=..
# Auto-generated by https://github.com/chrisvire/BuildUpdate.
# Do not edit this file by hand!

cd "$(dirname "$0")"

# *** Functions ***
force=0
clean=0

while getopts fc opt; do
case $opt in
f) force=1 ;;
c) clean=1 ;;
esac
done

shift $((OPTIND - 1))

copy_auto() {
if [ "$clean" == "1" ]
then
echo cleaning $2
rm -f ""$2""
else
where_curl=$(type -P curl)
where_wget=$(type -P wget)
if [ "$where_curl" != "" ]
then
copy_curl "$1" "$2"
elif [ "$where_wget" != "" ]
then
copy_wget "$1" "$2"
else
echo "Missing curl or wget"
exit 1
fi
fi
}

copy_curl() {
echo "curl: $2 <= $1"
if [ -e "$2" ] && [ "$force" != "1" ]
then
curl -# -L -z "$2" -o "$2" "$1"
else
curl -# -L -o "$2" "$1"
fi
}

copy_wget() {
echo "wget: $2 <= $1"
f1=$(basename $1)
f2=$(basename $2)
cd $(dirname $2)
wget -q -L -N "$1"
# wget has no true equivalent of curl's -o option.
# Different versions of wget handle (or not) % escaping differently.
# A URL query is the only reason why $f1 and $f2 should differ.
if [ "$f1" != "$f2" ]; then mv $f2\?* $f2; fi
cd -
}


# *** Results ***
# build: Transcelerator-win-default Pull requests (Transcelerator_TransceleratorWinDefaultPullRequests)
# project: Transcelerator
# URL: https://build.palaso.org/viewType.html?buildTypeId=Transcelerator_TransceleratorWinDefaultPullRequests
# VCS: https://github.com/sillsdev/Transcelerator.git [master]
# dependencies:
# [0] build: palaso-win32-feature/nuget Continuous (Libpalaso_PalasoWin32featureNugetContinuous)
#     project: libpalaso
#     URL: https://build.palaso.org/viewType.html?buildTypeId=Libpalaso_PalasoWin32featureNugetContinuous
#     clean: false
#     revision: 7.0.0
#     paths: {"Keyman*.dll"=>"lib/dotnet"}
#     VCS: https://github.com/sillsdev/libpalaso.git [refs/heads/feature/nuget]

# make sure output directories exist
mkdir -p ../lib/dotnet

# download artifact dependencies
copy_auto http://build.palaso.org/guestAuth/repository/download/Libpalaso_PalasoWin32featureNugetContinuous/7.0.0/Keyman10Interop.dll ../lib/dotnet/Keyman10Interop.dll
copy_auto http://build.palaso.org/guestAuth/repository/download/Libpalaso_PalasoWin32featureNugetContinuous/7.0.0/Keyman7Interop.dll ../lib/dotnet/Keyman7Interop.dll
copy_auto http://build.palaso.org/guestAuth/repository/download/Libpalaso_PalasoWin32featureNugetContinuous/7.0.0/KeymanLink.dll ../lib/dotnet/KeymanLink.dll
# End of script
